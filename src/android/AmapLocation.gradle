repositories {
    mavenCentral()
}
dependencies {
    //implementation 'com.amap.api:location:4.7.0'
    implementation fileTree(dir: 'src/main/libs', include: ['*.jar'])
    implementation fileTree(dir: 'src/main/libs/arm64-v8a', include: ['*.so'])
    implementation fileTree(dir: 'src/main/libs/armeabi', include: ['*.so'])
    implementation fileTree(dir: 'src/main/libs/armeabi-v7a', include: ['*.so'])
    implementation fileTree(dir: 'src/main/libs/x86', include: ['*.so'])
    implementation fileTree(dir: 'src/main/libs/x86_64', include: ['*.so'])
}
